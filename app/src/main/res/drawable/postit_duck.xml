<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Amarillo pato -->
    <solid android:color="#FFE066" />

    <!-- Bordes redondeados -->
    <corners android:radius="16dp" />

    <!-- Borde naranja -->
    <stroke
        android:width="2dp"
        android:color="#FFB703" />

    <!-- Padding interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />

</shape>